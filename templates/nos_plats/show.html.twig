{% extends 'base.html.twig' %}

{% block title %}NosPlats{% endblock %}

{% block body %}
    <h1>NosPlats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nos_plat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ nos_plat.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ nos_plat.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ nos_plat.price }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ nos_plat.date ? nos_plat.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ nos_plat.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nos_plats_index') }}">back to list</a>

    <a href="{{ path('nos_plats_edit', {'id': nos_plat.id}) }}">edit</a>

    {{ include('nos_plats/_delete_form.html.twig') }}
{% endblock %}
