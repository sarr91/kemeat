<form method="post" action="{{ path('nos_plats_delete', {'id': nos_plat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nos_plat.id) }}">
    <button class="btn">Delete</button>
</form>
